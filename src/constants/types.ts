export type CatalogItemType = {
    id: number;
    name: string;
    price: number;
    priceOld: number;
    image: string;
};

export type FooterLinkType = { path: string; title: string; svg: string };
